# Copyright 2022 Ubuntu
# See LICENSE file for licensing details.

# For a complete list of supported options, see:
# https://juju.is/docs/sdk/metadata-reference
name: database-integrator-operator
display-name: |
  TEMPLATE-TODO: fill out a display name for the Charmcraft store
description: |
  TEMPLATE-TODO: fill out the charm's description
summary: |
  TEMPLATE-TODO: fill out the charm's summary

# TEMPLATE-TODO: replace with containers for your workload (delete for non-k8s)
containers:
  database-integrator-operator:
    resource: database-integrator-operator-image

# TEMPLATE-TODO: each container defined above must specify an oci-image resource
resources:
  database-integrator-operator-image:
    type: oci-image
    description: OCI image for database-integrator-operator

provides:
  postgres-provider:
    interface: db-connection

requires:
  db:
    interface: pgsql
